rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 目前設定為只有認證用戶可讀寫
    // 若不需要登入驗證，可改成下方寬鬆規則（僅建議開發階段使用）
    match /{document=**} {
      allow read, write: if true; // ← 正式上線前請改成適合的規則
    }
  }
}
